# WTOS-1 Codex START HERE

This is the public entry point for the WTOS-1 Codex task.

PUBLIC START URL:
https://github.com/zdmor/PAT_stock/blob/master/reviews/WTOS1_CODEX_START_HERE.md

FULL TASK URL:
https://github.com/zdmor/PAT_stock/blob/master/reviews/WTOS1_CODEX_TASK.md

RAW TASK URL:
https://raw.githubusercontent.com/zdmor/PAT_stock/master/reviews/WTOS1_CODEX_TASK.md

TARGET PRIVATE REPOSITORY:
https://github.com/zdmor/WTOS

TARGET EXECUTION LOCATION:
Use the existing local private clone/worktree of `zdmor/WTOS` on this machine.

## Execution instruction

1. Read the FULL TASK URL above completely.
2. In the local private WTOS repository, fetch the latest remote `main`.
3. Read `SYSTEM_MANIFEST.yaml` first and follow the WTOS resolver exactly.
4. Create/use the dedicated review branch required by the full task.
5. Perform the review AND implement the required WTOS changes directly in that private WTOS branch.
6. Run the required tests and fix branch-caused failures.
7. Make durable commits for the actual WTOS file changes.
8. Push the WTOS work branch to `origin` after the work is complete and at useful checkpoints.
9. Do NOT stop after producing only a review report or design proposal. The authorization is to review -> modify -> test -> commit -> push.
10. Do NOT ask for ordinary confirmation before editing, committing, or pushing within the authorized work branch.
11. If one lane is blocked, record the blocker and continue the other safe lanes.
12. Return only after all safely executable work in the full task is exhausted.

## GitHub authentication / PAT

Use the machine's already-configured GitHub authentication. Prefer existing Git Credential Manager or `gh auth` state. If the private WTOS repository requires an existing local PAT-backed credential, it may be used locally for authentication only.

Never print, echo, display, copy, log, transmit, commit, or expose the PAT or any part of it.

## Hard boundary

You are authorized to modify, commit, and push the private WTOS WORK BRANCH.

You are NOT authorized to:
- merge to `main`;
- modify real ChatGPT WTOS platform automations;
- change Live Authority under `active/`;
- enable Broker Write;
- place broker orders;
- perform Production Cutover;
- consume an explicitly untouched holdout without separate authorization.

BRANCH IMPLEMENTATION != CURRENT ACTIVATION.
RESEARCH != LIVE AUTHORITY.

Now read the FULL TASK URL and execute it continuously.